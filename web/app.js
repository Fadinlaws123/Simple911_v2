const app=document.getElementById('app');const callerView=document.getElementById('callerView');const dispatchView=document.getElementById('dispatchView');const templateSelect=document.getElementById('templateSelect');const messageInput=document.getElementById('messageInput');const characterCount=document.getElementById('characterCount');const anonymousInput=document.getElementById('anonymousInput');const anonymousRow=document.getElementById('anonymousRow');const callList=document.getElementById('callList');let services=[];let currentService=null;let calls=[];let onDuty=false;let filter='active';let maxLength=500;
const resource=()=>typeof GetParentResourceName==='function'?GetParentResourceName():'simple911';const post=(name,data={})=>fetch(`https://${resource()}/${name}`,{method:'POST',headers:{'Content-Type':'application/json; charset=UTF-8'},body:JSON.stringify(data)});
function close(){app.classList.add('hidden');callerView.classList.add('hidden');dispatchView.classList.add('hidden')}document.querySelectorAll('[data-close]').forEach(b=>b.addEventListener('click',()=>post('close')));
function serviceById(id){return services.find(s=>s.id===id)}function applyTemplate(){const t=currentService?.templates.find(x=>x.id===templateSelect.value);if(!t)return;messageInput.value=t.message||'';updateCount()}function updateCount(){characterCount.textContent=`${messageInput.value.length} / ${maxLength}`}messageInput.addEventListener('input',updateCount);templateSelect.addEventListener('change',applyTemplate);
function openCaller(data){services=data.services||[];currentService=serviceById(data.serviceId)||services[0];maxLength=data.maxMessageLength||500;messageInput.maxLength=maxLength;document.getElementById('serviceShortLabel').textContent=currentService.shortLabel;document.getElementById('serviceLabel').textContent=currentService.label;document.getElementById('serviceDescription').textContent=currentService.description||'';document.getElementById('callerLocation').textContent=data.location||'Unknown Location';templateSelect.innerHTML=(currentService.templates||[]).map(t=>`<option value="${escapeHtml(t.id)}">${escapeHtml(t.category)} · ${escapeHtml(t.label)}</option>`).join('');anonymousRow.classList.toggle('hidden',!currentService.allowAnonymous);anonymousInput.checked=false;applyTemplate();app.classList.remove('hidden');dispatchView.classList.add('hidden');callerView.classList.remove('hidden')}
document.getElementById('submitCall').addEventListener('click',()=>{const message=messageInput.value.trim();if(!message){toast('Please enter call details.','error');return}post('submitCall',{serviceId:currentService.id,templateId:templateSelect.value,message,anonymous:anonymousInput.checked})});
function escapeHtml(v){return String(v??'').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]))}function statusLabel(s){return({unassigned:'Unassigned',claimed:'Claimed',responding:'Responding',resolved:'Resolved'})[s]||s}function renderCalls(){const shown=calls.filter(c=>filter==='all'||c.status!=='resolved');document.getElementById('activeCount').textContent=calls.filter(c=>c.status!=='resolved').length;document.getElementById('respondingCount').textContent=calls.filter(c=>c.status==='responding').length;if(!onDuty){callList.innerHTML='<div class="empty-state"><h3>You are off duty</h3><p>Go on duty to receive and manage dispatch calls.</p></div>';return}if(!shown.length){callList.innerHTML='<div class="empty-state"><h3>No calls to display</h3><p>For once, Los Santos appears to be behaving itself.</p></div>';return}callList.innerHTML=shown.map(c=>`<article class="call-card"><div class="call-top"><div><span class="call-number">${escapeHtml(c.serviceId)} · Call #${c.id}</span><h3>${escapeHtml(c.title)}</h3></div><span class="priority priority-${c.priority}">Priority ${c.priority}</span></div><div class="call-meta"><span class="meta-pill">${escapeHtml(c.location)}</span><span class="meta-pill">${escapeHtml(c.category)}</span><span class="meta-pill">${escapeHtml(statusLabel(c.status))}</span>${c.claimedByName?`<span class="meta-pill">${escapeHtml(c.claimedByName)}</span>`:''}</div><p class="call-message">${escapeHtml(c.message)}</p><div class="call-actions">${c.coords?`<button class="button secondary" data-action="waypoint" data-id="${c.id}">Waypoint</button>`:''}${c.status==='unassigned'?`<button class="button primary" data-action="claim" data-id="${c.id}">Claim</button>`:''}${c.status==='claimed'?`<button class="button warning" data-action="respond" data-id="${c.id}">Respond</button><button class="button secondary" data-action="unclaim" data-id="${c.id}">Unclaim</button>`:''}${c.status==='responding'?`<button class="button success" data-action="resolve" data-id="${c.id}">Resolve</button>`:''}</div></article>`).join('');callList.querySelectorAll('[data-action]').forEach(b=>b.addEventListener('click',()=>post('dispatchAction',{action:b.dataset.action,callId:Number(b.dataset.id)})))}
function openDispatch(data){calls=data.calls||[];onDuty=!!data.onDuty;updateDuty();renderCalls();app.classList.remove('hidden');callerView.classList.add('hidden');dispatchView.classList.remove('hidden')}function updateDuty(){const b=document.getElementById('dutyButton');b.textContent=onDuty?'Go Off Duty':'Go On Duty';b.classList.toggle('success',onDuty)}document.getElementById('dutyButton').addEventListener('click',()=>post('toggleDuty'));document.querySelectorAll('[data-filter]').forEach(b=>b.addEventListener('click',()=>{filter=b.dataset.filter;document.querySelectorAll('[data-filter]').forEach(x=>x.classList.toggle('active',x===b));renderCalls()}));
function toast(message,kind='info'){const stack=document.getElementById('toastStack');const el=document.createElement('div');el.className=`toast ${kind}`;el.textContent=message;stack.appendChild(el);setTimeout(()=>el.remove(),5000)}window.addEventListener('message',e=>{const d=e.data||{};if(d.action==='openCaller')openCaller(d);if(d.action==='openDispatch')openDispatch(d);if(d.action==='close')close();if(d.action==='toast')toast(d.message,d.kind);if(d.action==='syncCalls'){calls=d.calls||[];renderCalls()}if(d.action==='dutyChanged'){onDuty=!!d.onDuty;updateDuty();renderCalls()}});window.addEventListener('keydown',e=>{if(e.key==='Escape')post('close')});
